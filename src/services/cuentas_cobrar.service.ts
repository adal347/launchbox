import { Injectable } from '@angular/core';
import { AngularFireDatabase } from 'angularfire2/database';

@Injectable()
export class CuentasCobrarService {
  public createNewEntry(service) {
  	/*let box = {
  		id: service.box,
  		tipoId: service.box,
  		statusId: service.box
  	}
  	let statusBox = {
  		id: service.status,
  		nombre: service.status

  	}
  	let typoBox = {
  		id: service.typeBox,
  		nombre: service.typeBox
  	}
  	let inquilino = {
  		id:service.inquilino,
  		nombre:service.nameInquilino,
  		apellidos: service.lastNameInquilino
  	}*/
    console.log('createNewEntryService');
  }
}
